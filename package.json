{
  "name": "agent-orchestra",
  "private": true,
  "type": "module",
  "scripts": {
    "doctor": "node orchestra.mjs doctor",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
